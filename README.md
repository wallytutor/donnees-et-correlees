# Données et corrélées

Comprendre la Data Science d'ores et... maintenant.

## Environment

- [Quarto](https://quarto.org/)
- ...

## Build

- Generate the slides locally with the following command(s):

    ```bash
    quarto render --to revealjs

    # Not yet configured:
    # quarto render --to beamer
    # quarto render --to pptx
    ```

- Repository owners can also publish to GitHub pages with:

    ```bash
    quarto publish gh-pages --no-prompt --no-browser
    ```
